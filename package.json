{
  "name": "hoods-hookups",
  "version": "1.0.0",
  "description": "Drop servicing lead generation platform",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "node server/index.js",
    "client": "cd client && npm run dev",
    "install:all": "npm install && cd client && npm install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "concurrently": "^8.2.2"
  }
}
